/**
 * Nền tảng được hỗ trợ. Giai đoạn 1 chỉ có Facebook (document §1, §13).
 */
export type Platform = 'facebook';

/**
 * Loại nội dung thu thập. Giai đoạn 1: post và video (document §2, §8).
 */
export type ContentType = 'post' | 'video';
